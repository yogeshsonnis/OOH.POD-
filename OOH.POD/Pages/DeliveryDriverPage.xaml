<UserControl x:Class="OOH.POD.Pages.DeliveryDriverPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OOH.POD.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="Select the carrier shown on your parcel label" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,60"  Foreground="DeepSkyBlue"/>
        <ItemsControl ItemsSource="{Binding Carriers}"
          HorizontalAlignment="Center"
          VerticalAlignment="Top"
          Margin="0,0,0,0"
          Width="900"
          BorderThickness="0"
          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
          >
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                        <Button Margin="20" BorderBrush="LightGray" BorderThickness="2" Style="{StaticResource LogoButtonStyle}" Cursor="Hand" Command="{Binding DataContext.NavigateToRecipientCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}">

                        <StackPanel Width="150" Height="150" Margin="10" Orientation="Vertical">
                            <Image Source="{Binding LogoPath}" Height="150" Stretch="Uniform"/>
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>
